---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: occupation-classifier
  labels:
    app: occupation-classifier
    component: backend
spec:
  type: LoadBalancer
  selector:
    app: occupation-classifier
    component: backend
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
