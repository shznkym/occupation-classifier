---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: occupation-classifier
  labels:
    app: occupation-classifier
    component: frontend
spec:
  type: LoadBalancer
  selector:
    app: occupation-classifier
    component: frontend
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
